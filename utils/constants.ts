// MQTT Typing timeouts
export const TYPING_THROTTLE_MS = 1000;
export const TYPING_TIMEOUT_MS = 3000;
export const TYPING_EXPIRY_MS = 3000;

// Default MQTT configuration
export const DEFAULT_BROKER_URL = "wss://test.mosquitto.org";
export const DEFAULT_PORT = 8081;
export const DEFAULT_TOPIC_PREFIX = "spark-chat-room";

// localStorage keys
export const STORAGE_KEY_CONFIG = "mqtt-server-config";
export const STORAGE_KEY_USERNAME = "mqtt-chat-username";

// Message ID generation
export const MESSAGE_ID_RANDOM_LENGTH = 7;
